..\..\SSRunBatch\memo\[00]_要求データ.txt と同じ。

但し、最初のコマンドを REM SSRunBatch_Meta=TSR にすると常駐モードでバッチファイルを実行する。


- - -
常駐モード

応答ファイルを指定してはならない。

バッチファイルの終了を待たずに応答する。

WSSRBServer.exe (SSRBServer.exe /TSR-SERVER) の終了時もバッチファイルの終了を待たない。

	-----> SSRBServer.exe /TSR がラップする形で呼び出し、後片付けを行うので、正常に動作すればゴミファイルが残ることはない。
		SSRBServer.exe を強制終了したり、バッチファイル実行中にシャットダウンした場合、この限りではない。

応答データ
	応答ファイル数 = 0
	標準出力行数   = 1
	標準出力行[0]  = TSR OK

